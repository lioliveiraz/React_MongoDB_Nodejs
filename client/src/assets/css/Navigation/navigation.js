import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  root: {
    background: "#1A535C",
    color: "white",
  },
  subnav: {},
  links: { color: "white" },
  button: {
    position: "relative",
    right: "50%",
  },
}));
